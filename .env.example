# Copy this file to .env and fill in real values. .env itself is
# gitignored — never commit real keys.

# Required for the AI chatbot (/api/chat). Get a key at
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
